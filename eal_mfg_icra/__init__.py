"""Minimal, auditable EAL-MFG flocking prototype (artifact mirror).

This is the anonymous-review artifact subset of the ALF study.  Only the
simulator core, the paper's metric definitions, and the six regeneration
scripts are included, so the package deliberately does not re-export the
research modules of the full development tree.
"""

__all__: list[str] = []
